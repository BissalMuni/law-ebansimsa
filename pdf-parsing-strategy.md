# PDF 분석 전략

## 권장 도구: OpenDataLoader PDF (한컴)

> [GitHub](https://github.com/opendataloader-project/opendataloader-pdf) | Stars: 21.2k | License: Apache 2.0
> 벤치마크 1위 (정확도 0.907) — 읽기 순서, 테이블, 헤딩 추출 종합

한컴이 개발한 오픈소스 PDF 데이터 추출기. 2026년 3월 v2.0 공개, GitHub 트렌딩 1위 달성.

### 설치

```bash
pip install -U opendataloader-pdf

# 고급 기능 (OCR, 수식 등)
pip install -U "opendataloader-pdf[hybrid]"
```

### 핵심 기능

| 기능 | 설명 |
|------|------|
| 텍스트 추출 | 올바른 읽기 순서 보장 |
| 테이블 추출 | 병합 셀, 중첩 테이블, 경계선 없는 표 지원 |
| 헤딩 계층 감지 | 자동 목차 구조 파악 |
| OCR | 스캔 PDF 지원 (80+ 언어, 한국어 포함) |
| 수식 추출 | LaTeX 변환 |
| 차트/이미지 | AI 기반 설명 생성 |
| Bounding Box | 모든 요소에 좌표 제공 |

### 출력 포맷

| 포맷 | 용도 |
|------|------|
| **Markdown** | LLM 컨텍스트, RAG 청킹에 최적 |
| **JSON** | 바운딩 박스 + 시맨틱 타입 포함 구조화 데이터 |
| **HTML** | 웹 표시 |
| **Annotated PDF** | 감지된 구조 시각적 디버깅 |

### 기본 사용법

```python
import opendataloader_pdf

opendataloader_pdf.convert(
    input_path=["2022 ebansimsa.pdf", "2025 jungbigijun.pdf"],
    output_dir="output/",
    format="markdown,json"
)
```

```bash
# CLI
opendataloader-pdf "2022 ebansimsa.pdf" "2025 jungbigijun.pdf"
```

---

## 기존 도구 (대안)

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| PyMuPDF (fitz) | 1.27.1 | 빠른 텍스트 추출 (단순 구조) |
| pdfplumber | 0.11.9 | 테이블 추출 |
| pypdf | 6.11.0 | 기본 텍스트/메타데이터 |
| pdfminer.six | — | 레이아웃 기반 추출 |

---

## 파싱 파이프라인

```
PDF 원본 (Raw Source)
    │
    ▼
[1단계] OpenDataLoader로 마크다운 변환              ← 스크립트
    │
    ▼
[1.5단계] 0.1.x 문서 생성                            ← LLM (원본 md 앞부분 직접 확인)
    │   - 원본 md에서 목차 이전 콘텐츠 확인 (서문, 일러두기, 길라잡이 등)
    │   - 해당 내용을 0.1.1, 0.1.2, ... 파일로 생성
    │   - 스크립트가 아닌 LLM이 직접 판단
    │
    ▼
[2단계] 원본 목차 생성 → 0.2.1-목차.md              ← LLM (원본 md 읽고 작성)
    │   - PDF의 헤딩 구조를 그대로 추출
    │   - 페이지 번호 포함
    │
    ▼
[2.5단계] 코드 목차 생성 → 0.2.2-목차_코드.md      ← LLM
    │   - 목차 기반으로 각 헤딩에 계층적 코드(ID) 부여
    │   - 이후 모든 참조는 이 코드로 수행
    │   - 코드 형식: 1.2.3 (편.장.절 또는 편.장.항목)
    │
    ▼
[2.6단계] 라인맵 초안 생성 → 0.2.3-라인맵.md       ← 스크립트 (step1_generate_linemap.py)
    │   - toc-coded의 각 헤딩 텍스트를 원본 md에서 검색
    │   - 페이지 힌트 기반 추정 위치 근처에서 검색
    │   - 결과: | 코드 | 제목 | 시작 줄 | 종료 줄 | 줄 수 | (5열)
    │   - 대목차(내용 없는 상위 항목)는 **볼드** 구분 행, 시작/종료 줄 비워둠
    │   - 내용 있는 대목차는 **볼드** + 시작/종료 줄 기입
    │   - 못 찾은 항목은 status: ✗ not_found 표시
    │
    ▼
[2.7단계] 라인맵 검증·보정                          ← 스크립트 + LLM
    │   - step1_5_verify_linemap.py로 자동 검증 → 0.2.4-라인맵_검증.md 생성
    │   - 검증 테이블: | 코드 | TOC 제목 | 시작줄 | 원본 텍스트 | 유사도 | 결과 | (6열)
    │   - 시작줄의 원본 텍스트와 TOC 제목을 비교하여 ✅/⚠️/❌ 판정
    │   - ❌ 불일치 항목을 LLM이 수동 보정
    │   - not_found 항목을 원본 md에서 직접 찾아 보정
    │   - 잘못 매칭된 항목 수정 (각주, 꼬리말, 목차 참조를 잡은 경우)
    │   - 종료줄 = 다음 항목 시작줄 - 1 (자동 계산, 수동 보정 시 연쇄 수정 필요)
    │   - 줄수가 비정상(0~2줄)인 항목은 반드시 검증
    │   - 시작줄의 실제 내용이 헤딩(####)인지 확인
    │   - 라인맵 100% 완성 후에만 다음 단계 진행
    │   - ⚠️ 사용자가 라인맵 검증 결과를 확인·승인한 후에만 3단계 진행
    │
    ▼
[3단계] 위키 파일 생성                              ← 스크립트 (step2_generate_wiki.py)
    │   - 100% 검증된 라인맵의 시작줄~종료줄로 원본 md 슬라이싱
    │   - 파일명: <헤딩코드>-<헤딩제목>.md
    │   - 예: 1.2.3-입법의_필요성_및_가능_여부_판단.md
    │
    ▼
[4단계] index.md 업데이트 + 상호참조                ← LLM
```

### 스크립트 vs LLM 역할 분담

| 단계 | 담당 | 이유 |
|------|------|------|
| 1 (파싱) | 스크립트 | OpenDataLoader가 정확히 수행 |
| 2, 2.5 (목차·코드) | LLM | 구조 판단 필요, 정확도 우선 |
| 2.6 (라인맵 초안) | 스크립트 | 대량 검색, 토큰 절약 |
| 2.7 (라인맵 보정) | LLM | 못 찾은 항목 수동 보정, 100% 보장 |
| 3 (위키 생성) | 스크립트 | 검증된 라인맵 기준 단순 슬라이싱 |
| 4 (인덱스) | LLM | 상호참조 판단 필요 |

### 헤딩 코드 체계

모든 파일은 wiki/ 폴더에 위치하며, 헤딩 코드로 구분한다:

```markdown
# 코드 체계

0.x = 본문 이전 콘텐츠 + 목차 관련

  0.1 = 목차 이전 문서 (서문, 일러두기, 길라잡이 등)
    - 0.1.1 길라잡이
    - 0.1.2 일러두기
    - 0.1.3 ...

  0.2 = 목차 관련
    - 0.2.1 목차 (원본)
    - 0.2.2 목차 코드 (헤딩 코드 부여된 버전)
    - 0.2.3 라인맵

1.x ~ = 본문 콘텐츠 (실제 내용)
  - 1 제1편 자치법규 입안의 기본원칙
    - 1.1 제1장 자치법규 입안의 의의
    - 1.2 제2장 입법의 필요성 판단과 입법 형식의 선택
      - 1.2.1 자치법규 관계 법령
      - 1.2.2 자치법규의 종류
      - 1.2.3 입법의 필요성 및 가능 여부 판단
      - 1.2.4 입법형식의 선택
    - 1.3 제3장 자치법규 입안의 기본원칙
      - 1.3.1 소관사무의 원칙
      - 1.3.2 법령 우위의 원칙
```

### 위키 파일명 규칙

```
<헤딩코드>-<헤딩제목>.md

- 헤딩코드와 헤딩제목은 대시(-)로 연결
- 한글 제목의 공백은 언더바(_)로 처리
- 영어 제목은 kebab-case (언더바로 연결)

예:
1.2.4-입법형식의_선택.md
1.3.1-소관사무의_원칙.md
2.1.2-목적규정.md
2.2.3-공유재산의_관리처분.md
```

### 원칙

- 헤딩 코드는 toc.md에서 **한 번만** 부여하고, 이후 모든 곳에서 이 코드로 참조
- 위키 파일명에 코드를 선행하여 정렬 순서 = 원본 순서 보장
- 상호참조 시: `[목적규정](2.1.2-목적규정.md)` 형식

## 디렉토리 구조 규칙

```
[루트] (원본, 불변)
├── *.pdf                           ← Raw Source (수정 금지)
├── pdf-parsing-strategy.md         ← 이 문서 (파이프라인 원칙)
├── CLAUDE.md
└── index.md                        ← [5단계] 전체 인덱스

output/ (문서별 자족적 구조, 전부 재생성 가능)
├── <문서명>/
│   ├── <문서명>.md                 ← [1단계] OpenDataLoader 마크다운
│   ├── <문서명>.json               ← [1단계] OpenDataLoader JSON
│   ├── <문서명>_images/            ← [1단계] 추출된 이미지
│   └── wiki/                       ← [2~4단계] 모든 위키 페이지
│       ├── 0.1-목차.md             ← [2단계] 원본 목차
│       ├── 0.2-목차_코드.md        ← [2.5단계] 코드 부여 목차
│       ├── 0.3-서문.md             ← 본문 이전 콘텐츠
│       ├── 1.1-자치법규_입안의_의의.md  ← [3~4단계] 본문
│       ├── 1.2.1-자치법규_관계_법령.md
│       └── ...
└── ...
```

### 디렉토리 원칙

- `*.pdf` 원본은 루트에 위치, 절대 수정하지 않음
- `output/<문서명>/`은 해당 PDF의 모든 산출물을 포함 (파싱 결과 + 목차 + 위키)
- 문서별로 완전히 자족적: 새 PDF 추가 시 `output/<문서명>/` 하나만 생성
- `output/`은 언제든 재생성 가능 (PDF만 있으면 됨)
- `index.md`는 루트에 위치하여 전체 문서를 가로지르는 인덱스 역할

## 구현 스크립트 (예정)

`scripts/parse_pdf.py` — OpenDataLoader 기반 PDF → 섹션별 마크다운 변환
