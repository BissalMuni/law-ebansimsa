# PDF 분석 전략

## 사용 가능한 도구

현재 시스템에 설치된 PDF 파싱 라이브러리:

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **PyMuPDF (fitz)** | 1.27.1 | 텍스트 추출, 페이지별 파싱, 테이블 감지 |
| **pdfplumber** | 0.11.9 | 테이블 추출에 특화 |
| **pypdf** | 6.11.0 | 기본 텍스트 추출, 메타데이터 |
| **pdfminer.six** | — | 레이아웃 기반 텍스트 추출 |
| **pdftotext** | (Git 내장) | CLI 기반 빠른 텍스트 추출 |

## 파싱 파이프라인

```
PDF 원본 (Raw Source)
    │
    ▼
[1단계] 목차(TOC) 추출 → toc.md 생성
    │
    ▼
[2단계] 페이지 범위 매핑 (목차 항목 ↔ 페이지 번호)
    │
    ▼
[3단계] 섹션별 텍스트 추출 (PyMuPDF)
    │
    ▼
[4단계] 위키 페이지 생성 (LLM이 구조화)
    │
    ▼
[5단계] index.md 업데이트 + 상호참조
```

## 제안하는 접근 방식

### 방법 1: PyMuPDF 기반 (권장)
- `fitz.open()` → 페이지별 `get_text()` 로 전체 텍스트 추출
- 정규식으로 제/장/절/조 패턴 감지하여 구조 파악
- 장점: 빠르고, 한글 지원 우수, 레이아웃 보존

### 방법 2: pdfplumber (테이블이 많을 경우)
- 표 형태의 데이터(용어 목록, 정비 목록 등)에 적합
- `page.extract_tables()` 로 구조화된 데이터 추출

### 방법 3: 하이브리드
- 본문 텍스트 → PyMuPDF
- 표/목록 → pdfplumber
- 최종 구조화 → LLM

## 구현 스크립트 (예정)

`scripts/parse_pdf.py` — PDF를 섹션별 마크다운으로 변환하는 스크립트
