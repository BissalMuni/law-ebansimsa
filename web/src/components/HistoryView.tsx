"use client";

// 이력·시간여행 (US8) — T033 에서 Snapshot 타임라인 구현
export function HistoryView({ projectId: _projectId }: { projectId: string }) {
  return (
    <p className="p-3 text-sm text-muted-foreground">이력을 불러오는 중…</p>
  );
}
