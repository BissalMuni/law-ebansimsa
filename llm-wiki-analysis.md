# Andrej Karpathy's LLM Wiki 분석

> 출처: [karpathy/llm-wiki (GitHub Gist)](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)

## 1. 핵심 개념

LLM Wiki는 **RAG(Retrieval-Augmented Generation)의 대안**으로, LLM이 원본 문서를 매번 재검색하는 대신 **구조화된 마크다운 위키를 점진적으로 구축·유지**하는 패턴이다.

핵심 차이점: **지식이 누적(compounding)** 된다. 한번 소스를 인제스트하면 연결 관계가 수립되고, 모순이 플래그되며, 합성이 완료된다. 이후 쿼리는 매번 관계를 재발견하는 대신 축적된 구조를 활용한다.

## 2. 3계층 아키텍처

```
┌─────────────────────────────────────┐
│  Schema (설정)                       │  ← CLAUDE.md 등 위키 구조·워크플로우 정의
├─────────────────────────────────────┤
│  Wiki (LLM 소유)                     │  ← 마크다운 파일: 요약, 엔티티, 개념, 상호참조
├─────────────────────────────────────┤
│  Raw Sources (불변)                  │  ← 원본 문서: PDF, 논문, 기사 등
└─────────────────────────────────────┘
```

| 계층 | 역할 | 소유자 |
|------|------|--------|
| **Raw Sources** | 진실의 원천(Single Source of Truth). 큐레이션만 하고 수정하지 않음 | 사람 |
| **Wiki** | 요약, 엔티티 페이지, 개념 페이지, 상호참조를 담은 마크다운 파일 | LLM |
| **Schema** | 위키 구조, 컨벤션, 소스 인제스트·유지보수 워크플로우를 명시하는 설정 문서 | 사람 |

## 3. 핵심 오퍼레이션

### Ingest (인제스트)
새 소스가 추가되면 단순 인덱싱이 아니라 **10~15개 관련 페이지를 동시에 업데이트**한다.

1. 소스를 읽고 핵심 내용 추출
2. 요약 페이지 작성
3. 인덱스 업데이트
4. 기존 관련 페이지 수정·보완

### Query (쿼리)
위키 페이지를 검색하여 질문에 답변하고, 가치 있는 탐색 결과는 **새 페이지로 환원**하여 미래 지식을 누적시킨다.

### Lint (린트)
주기적 건강 점검:
- 모순 탐지
- 고아(orphan) 페이지 발견
- 누락된 상호참조 보완
- 데이터 갭 식별

## 4. 지원 인프라

| 파일 | 역할 |
|------|------|
| `index.md` | 카테고리별로 정리된 위키 페이지 목록 (콘텐츠 카탈로그) |
| `log.md` | 인제스트, 쿼리, 유지보수의 시간순 추가 전용(append-only) 기록 |

## 5. 이 패턴이 작동하는 이유

> "LLMs don't get bored, don't forget to update a cross-reference, and can touch 15 files in one pass."

기존 위키가 실패하는 이유는 **유지보수 부담** 때문이다. LLM Wiki에서는:
- 사람 → 소스 큐레이션 + 사고
- LLM → 상호참조 업데이트, 모순 플래그, 일관성 유지 등 반복 작업

## 6. 비유

> **"Obsidian은 IDE, LLM은 프로그래머, Wiki는 코드베이스"**

사용자가 위키를 직접 작성하는 것이 아니라, 소스를 제공하고 방향을 설정하면 LLM이 구조적 작업을 처리한다.

## 7. 적용 분야

- 개인 지식 관리 (목표, 건강 등)
- 연구 딥다이브
- 독서 동반자
- 비즈니스 팀 위키
- 경쟁 분석
- **법령 지식 베이스** ← 본 프로젝트
